({
    doInit : function (component, event, helper) {
        helper.workIcon(component, event, helper);
    },
    
    editRecord : function (component, event, helper){
        helper.editRecordHelper(component, event, helper);
    },
    
    openRecord : function (component, event, helper) {
        helper.openRecordHelper(component, event, helper);
    },
})